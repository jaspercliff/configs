% git, version control

# 将指定分支的文件同步到当前分支
git restore --source <branch> <file>

# 添加远程仓库
git remote add <name> <url>

# 查看远程仓库地址  
git remote -v

# 将远程仓库地址https换成ssh 
git remote set-url <name> <ssh_url>

# 对比文件在不同分支的差异 git diff mac master -- cheats/git.cheat
git diff <分支A> <分支B> -- <文件路径>
