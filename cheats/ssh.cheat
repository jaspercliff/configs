% ssh,ssh

# 生成ssh密钥 -t 密钥类型 -b 长度 4096 -C comment
ssh-keygen -t rsa -b 4096 -C "shamsjasper@gmail.com"

# 生成ssh密钥 ed25519  密钥长度固定 256 位
ssh-keygen -t ed25519 -C "shamsjasper@gmail.com"

# 禁止分配终端（no TTY）只测试认证 user@host  github 默认用户git
ssh -T git@github.com
